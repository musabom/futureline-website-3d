I need to integrate Thawani Pay as my payment gateway. Here are the verified details from Thawani's official documentation — implement exactly this, no assumptions.
Environment Variables already in Replit Secrets:

THAWANI_SECRET_KEY = UAT secret key
THAWANI_PUBLISHABLE_KEY = UAT publishable key
THAWANI_MODE = test
THAWANI_BASE_URL = https://uatcheckout.thawani.om

Create a thawaniService.js file in the server folder with these functions:

createCheckoutSession({ orderId, customerName, products, userId }) — calls POST /api/v1/checkout/session on Thawani API with thawani-api-key header using the secret key. Products have name, unit_amount (in Baisa, 1 OMR = 1000 Baisa), and quantity. Set success_url to https://futureline.ai/payment/success and cancel_url to https://futureline.ai/payment/cancel. Return the session_id and the redirect URL.
verifyPayment({ sessionId }) — calls GET /api/v1/checkout/session/{sessionId} with the secret key header and returns the payment_status.

Create a paymentRoutes.js file with these routes:

POST /payment/create-session — protected route (logged-in customers only), calls createCheckoutSession, saves the session_id to the database against the order, and returns the Thawani redirect URL to the frontend.
GET /payment/success — called when Thawani redirects back after payment. Must extract session_id from query params, call verifyPayment, confirm payment_status === "paid" SERVER-SIDE before marking the order as paid in the database. Never trust the frontend alone for this. Then send confirmation email using sendPaymentConfirmationEmail from emailService.js.
GET /payment/cancel — handles cancelled payments, updates order status to cancelled.

Security rules to follow:

Secret key is ONLY used on the backend — never sent to the frontend
Payment status MUST be verified server-side via the Thawani API — never trust what the frontend sends
Save session_id to the database when created so you can verify it on return
Add the payment routes to server/index.js

After implementation:

Show me the complete thawaniService.js and paymentRoutes.js
Show me what was added to server/index.js
Tell me what frontend changes are needed to trigger the payment flow